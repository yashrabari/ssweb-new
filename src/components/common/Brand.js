import styled from "styled-components"

const Brand = styled.h2`
  font-family: "TT Commons";
  font-style: normal;
  font-weight: 700;
  font-size: 24px;
  line-height: 28px;

  margin: 12px;

  color: #022714;
`

export default Brand
